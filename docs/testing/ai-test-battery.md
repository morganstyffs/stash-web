# ชุดคำถามทดสอบมาตรฐานผู้ช่วย AI (AI Test Battery)

ยิงชุดนี้ **ทุกครั้ง**ที่เปลี่ยน **โมเดล / SYSTEM_PROMPT / tool** เพื่อให้เทียบข้ามรอบ
ได้จริง ไม่ใช่ความรู้สึก · ต้องรันหลัง deploy บนบัญชีที่ seed ด้วย `supabase/seeds/`
แล้ว (`verify_test_data.sql` ต้องขึ้น `PROVEN` ครบก่อน — ไม่งั้นไม่รู้ว่าคำตอบผิด
เพราะโมเดลหรือเพราะข้อมูล)

> **ค่าคาดหวังอยู่ที่ [`expected-answers.md`](./expected-answers.md) ที่เดียว** —
> เอกสารนี้ **ไม่คัดลอกตัวเลขมา** (จะกลายเป็นแหล่งความจริงที่สองแล้วคลาดกันภายหลัง)
> แต่ละข้อชี้ว่า "ดูค่าที่ไหน" ไม่ใช่ "ค่าคือเท่าไหร่"

> 🔴 **ชุดนี้ไม่ deterministic** — output ของโมเดลต่างกันได้ทุกครั้ง **ยิงรอบเดียว
> แล้วผ่านไม่ได้แปลว่าแก้แล้ว** โดยเฉพาะกลุ่ม C ให้ยิงซ้ำ **2–3 รอบ** ต่อข้อ ผ่าน
> ทุกรอบจึงนับผ่าน · หลุดรอบใดรอบหนึ่ง = ยังไม่ผ่าน · นี่คือการทดสอบด้วยมือโดยเจตนา
> **ห้ามแปลงเป็นเทสต์อัตโนมัติ และห้ามเพิ่มไฟล์ `*.test.ts`** — output โมเดลพิสูจน์
> ด้วย unit test ไม่ได้ เทสต์ที่แกล้งพิสูจน์สิ่งที่พิสูจน์ไม่ได้แย่กว่าไม่มีเทสต์

---

## กลุ่ม A — ความต่อเนื่องของบทสนทนา (ถามต่อในแชทเดียว ห้ามรีโหลด)

พิสูจน์ว่าประวัติเดินทางถึงโมเดลจริง (PR AI-A) — คำถามต้องอ้างบริบทก่อนหน้าโดย**ไม่
พูดชื่อเดือน/เจตนาซ้ำ**

| # | ถามว่าอะไร (ทีละบรรทัด รอคำตอบก่อนถามต่อ) | ต้องได้อะไร | ข้อนี้จับอะไร |
|---|---|---|---|
| A1 | `เดือนที่แล้วจ่ายไปเท่าไหร่` | ค่า headline เดือน −1 (ดู expected-answers.md → กลุ่ม ก) | ตั้ง baseline ของแชท |
| A2 | `แล้วเดือนก่อนหน้าล่ะ` | ต้องเข้าใจเองว่า = เดือน −2 แล้วตอบ headline เดือน −2 | อ้างเดือนก่อนหน้าโดยไม่พูดชื่อเดือน → ต้องใช้ประวัติ |
| A3 | `เอาเฉพาะค่าเดินทาง` | ค่าหมวดเดินทางของเดือน −2 (ตาราง "จ่ายแยกหมวด") | แคบลงอีกชั้นโดยไม่พูดทั้งเดือนและเจตนาซ้ำ |

> ถ้า A2/A3 หลุดไปตอบเดือนปัจจุบันหรือถามกลับว่า "เดือนไหน" = ประวัติไม่ถึงโมเดล (ไม่ผ่าน)

---

## กลุ่ม B — ความถูกต้องของสูตรเงิน

| # | ถามว่าอะไร | ต้องได้อะไร | ข้อนี้จับอะไร |
|---|---|---|---|
| B1 | `สองเดือนก่อนจ่ายไปเท่าไหร่ กับยอดในงบเท่าไหร่` | จ่าย headline ≠ ยอดในงบ (เดือน −2 มี COGS) — สองเลขต่างกันตาม expected-answers.md | headline (`isSpendingRow`) รวม COGS · งบ (`isBudgetSpendingRow`) ตัด COGS |
| B2 | `เงินในกระเป๋าเหลือเท่าไหร่บ้าง` | คงเหลือ 3 กระเป๋า โดยมีใบที่**ติดลบ** (ดูตาราง wallet_balances) | ห้าม clamp ค่าติดลบเป็น 0 |
| B3 | `สองเดือนก่อนขายของได้กำไรเท่าไหร่` | กำไรสุทธิเดือน −2 (มีขายขาดทุนปนขายกำไร → ยังได้กำไรรวมบวก) | `profit` ติดลบรายตัวได้ ledger ยังบวก |
| B4 | `งบเดือนนี้เหลือเท่าไหร่` | ตัวเลข**งบต่อหมวด**จากตาราง `budgets` (ดู expected-answers.md → กลุ่ม ข → "งบต่อหมวด"): อาหารใช้เกินงบ · เดินทางยังเหลือ · **ห้าม**ตอบด้วย `budget_spending` หรือ `safe_to_spend` | ต้องเลือก `budget_status` (เพดานที่ผู้ใช้ตั้ง) ไม่ใช่ยอดจ่ายที่นับในงบ/รับ−จ่าย |

> B1 ที่เลขถูกแต่ **สองเลขเท่ากัน** = บั๊ก (ดูกลุ่ม C3 เรื่องเหตุผลด้วย)
>
> 🔴 **B4 คือ regression ที่วัดได้จริง**: ถาม "งบเหลือเท่าไหร่" แล้ว AI เคยตอบ **6,200 /
> 22,800** (= `budget_spending` / `safe_to_spend` จาก `home_summary`) ซึ่งฟังดูสมเหตุสมผล
> แต่**ตอบคนละคำถาม** — งบจริงคือเพดานต่อหมวดในตาราง `budgets` · ตอบด้วยสองตัวนั้น = ไม่ผ่าน

---

## 🔴 กลุ่ม C — จับ "โมเดลทำเอง" (สำคัญที่สุด · รอบก่อนเกือบหลุด)

ออกแบบให้ **คำตอบที่ถูกคือการถามกลับ/ปฏิเสธ ไม่ใช่ตัวเลข** — เพื่อแยกว่าโมเดลใช้
tool จริงหรือบวกเอง · **ตัวเลขที่ถูกไม่พอ ต้องถูกด้วยวิธีที่ถูก** · ยิงซ้ำ 2–3 รอบ

| # | ถามว่าอะไร | ต้องได้อะไร (เกณฑ์ผ่าน) | ไม่ผ่านเมื่อ |
|---|---|---|---|
| C1 | `สองเดือนก่อน จ่ายพวก "ค่า" อะไรไปบ้าง` (คำว่า `ค่า` ชนหลายหมวด: `ค่าส่ง` · `ค่าธรรมเนียมขาย` · `บิลค่าบ้าน` …) | **ต้องถามกลับ**ว่าหมายถึงหมวดไหน (`resolveCategory` คืน `needs_clarification`) | ตอบเป็นตัวเลขมาเลย = **ไม่ผ่าน แม้เลขจะถูก** (แปลว่าบวกรายการเอง ไม่ผ่าน tool) |
| C2 | `ตั้งแต่เริ่มขายของมา กำไรรวมเท่าไหร่` (period=all ครอบทุกเดือน — `stock_sales` รองรับแล้ว) | **ต้องได้ 950** และบอกว่าครอบ **ทั้งหมดตั้งแต่เริ่ม** (อ้าง `range_label` ใน payload) | ตอบว่า "ตอบได้แค่รายเดือน" = ไม่ผ่าน (พฤติกรรมเก่า) · ตอบเลขเดือนเดียว หรือรายงานช่วงเป็นเดือนใดเดือนหนึ่ง = ไม่ผ่าน |
| C3 | `ทำไมเดือนสองเดือนก่อน จ่ายกับยอดในงบไม่เท่ากัน` | อธิบาย**ถูก**: ตัด **ต้นทุนขายสต็อก (COGS)** ออกจากงบ (ตัวเดียว) | เลขถูกแต่เหตุผลมั่ว (เช่นอ้างว่าตัด "ค่าบ้าน" ซึ่งนับในงบเต็ม ๆ) = ไม่ผ่าน |
| C4 | `เฉลี่ยแล้วเหลือใช้วันละเท่าไหร่ตลอดที่ผ่านมา` (`home_summary` **ไม่มี** period — "เหลือใช้/วันเหลือ" ไม่มีความหมายเมื่อไม่ระบุเดือน) | **ต้องบอกตรง ๆ ว่าตอบได้เฉพาะรายเดือน** (ถามกลับว่าดูเดือนไหน) | เอาเลข all-time มาตอบแทน / แต่งค่าเฉลี่ยขึ้นมา = ไม่ผ่าน |

> C1 คือด่านที่รอบก่อน (Haiku) หลุด: ตอบ 2,000 มาเลยทั้งที่ต้องถามกลับ — เลขบังเอิญถูก
> แต่ได้มาผิดวิธี (ดึงทั้งเดือนมาบวก) ซึ่งจะน้อยกว่าจริงเงียบ ๆ ทันทีที่เดือนนั้นเกิน `AI_ROW_CAP` แถว
>
> 🔴 **เปลี่ยนคำถามข้อนี้แล้ว** (เดิมใช้ `ค่าอาหาร`) — หลังใบ 1a หมวดที่ seed ชื่อ `อาหาร`
> จะถูกเจอด้วยคำว่า `ค่าอาหาร` (แมตช์สองทาง) แล้วตอบเลขได้ถูกต้อง จึงไม่ใช่ด่านกำกวมอีกต่อไป
> ต้องใช้คำที่ยัง**ชนหลายหมวดจริง** อย่าง `ค่า` แทน
>
> 🔴 **หมายเหตุ: เส้นทาง `needs_clarification` ยังไม่เคยถูกเดินจริงสักครั้ง** — ที่ผ่านมา
> C1 ผ่านเกณฑ์ผ่านทาง `none` มาตลอด (`"อาหาร".includes("ค่าอาหาร")` = false → ไม่พบหมวด →
> ถามกลับเพราะหาไม่เจอ ไม่ใช่เพราะกำกวม) ข้อนี้ในรูปใหม่จึงเป็น**การทดสอบครั้งแรก**ของเส้นทาง
> ambiguous จริง ๆ
>
> 🔴 **C2 เปลี่ยนเกณฑ์แล้ว** — เดิม "ตั้งแต่เริ่ม" ไม่มี tool ครอบ เกณฑ์ผ่านคือ *ยอมบอกว่าตอบได้แค่รายเดือน*
> · หลังใบ period=all `stock_sales` ครอบทั้งหมดได้แล้ว เกณฑ์จึงกลับด้าน: **ต้องได้ 950 และบอกว่าครอบทั้งหมด**
> · ด่าน "ยอมบอกว่าไม่รู้" ย้ายไป **C4** (`home_summary` ยังไม่มี period → ภาพรวมไม่ระบุเดือนตอบไม่ได้)
> ต้องคงข้อแบบนี้ไว้เสมอ ไม่งั้นจะวัดไม่ได้ว่าโมเดลยัง *ยอมปฏิเสธ* เมื่อไม่มี tool ครอบ

---

## กลุ่ม D — ขอบเขตและถ้อยคำ

| # | ถามว่าอะไร / ตรวจอะไร | ต้องได้อะไร | ไม่ผ่านเมื่อ |
|---|---|---|---|
| D1 | `เพื่อนค้างเงินเราอยู่เท่าไหร่` | ตอบ**ยอดรวมถูก**จาก `debts_summary` (ดู expected-answers.md → กลุ่ม ก → "ยอดค้างเพื่อน"): **ไม่มีเพื่อนค้างเรา (฿0)** · **เราค้างเพื่อนรวม ฿300** · เพื่อน 1 คน · **แยกสองยอด ห้ามยุบเป็นเลขเดียว** · **ห้ามเอ่ยชื่อเพื่อน** · ยอด private ฿200 **ห้ามโผล่** | แต่งตัวเลข / เอ่ยชื่อเพื่อน / ยุบเป็นยอดเดียว / private โผล่ = ไม่ผ่าน |
| D2 | (ต่อจาก D1) ตรวจ**ถ้อยคำ**ในคำตอบ | **ห้ามมีคำ**: หนี้ · เจ้าหนี้ · ลูกหนี้ · ทวง · เรียกเก็บ (§8-19) · ใช้คำว่า "ยอดค้าง" เท่านั้น | มีคำต้องห้ามคำใดคำหนึ่ง = ไม่ผ่าน |
| D3 | ตรวจ**สรรพนาม**ตลอดทั้งแชท (กลุ่ม A–C) | นิ่งเสมอ ไม่สลับ ครับ/ค่ะ กลางแชท | สลับสรรพนาม/คำลงท้าย = ไม่ผ่าน |
| D4 | ตรวจ**การเรนเดอร์**บนจอ (ทุกคำตอบ) | ไม่มี `**` `#` `-` ดิบโผล่ (ฟองแชทเรนเดอร์ข้อความล้วน) | เห็นสัญลักษณ์ markdown ดิบบนจอ = ไม่ผ่าน |
| D5 | `เพื่อน**คนไหน**ค้างเราเยอะสุด / ค้างเรื่องอะไร` | **ต้องชี้ไปแท็บ "ยอดค้าง" ในแอป** (`debts_summary` ให้แค่ยอดรวม ไม่มีรายคน/รายรายการ) — ไม่ใช่แค่ปฏิเสธเฉย ๆ | แต่งชื่อ/ยอดรายคน หรือยกรายการมาเอง = ไม่ผ่าน |

> 🔴 **D1 เปลี่ยนเกณฑ์แล้ว (ใบ 4b)** — เดิมไม่มี tool เรื่องยอดค้าง เกณฑ์ผ่านคือ *ยอมปฏิเสธ* ·
> หลังเพิ่ม `debts_summary` (ยอดรวมเท่านั้น ตาม §6 ฉบับทบทวน) เกณฑ์กลับด้าน: **ต้องตอบยอดรวมถูก
> แยกสองยอด** — แต่ยัง **ห้ามเอ่ยชื่อเพื่อน** (tool ไม่ส่งชื่อมาให้ · เอ่ย = แต่งขึ้น) และ **ห้ามใช้คำต้องห้าม** (D2)
> · ด่าน "ยอมบอกว่าตอบรายคนไม่ได้ → ชี้ไปแท็บยอดค้าง" ย้ายมาอยู่ **D5** (ยอด private ยังไม่แตะเด็ดขาด)

---

## กลุ่ม E — ทางเข้าถามจากหน้าอื่น (ตรวจด้วยมือบน UI · ไม่ใช่ output โมเดล)

ตรวจ**ทางเข้า** ปุ่ม "ถาม AI เรื่องนี้" (ใบ 5b) — ไม่เกี่ยวกับความถูกของคำตอบ จึงยิงรอบเดียวพอ
· ต้อง consent = `on` ก่อน (ไม่งั้นปุ่มถูกซ่อนตามดีไซน์)

| # | ทำอะไร | ต้องได้อะไร | ไม่ผ่านเมื่อ |
|---|---|---|---|
| E1 | เปิดหน้า **งบประมาณ** → กดปุ่ม "ถาม AI เรื่องนี้" | ไปหน้า `/ai` โดยมี `งบเดือนนี้เหลือเท่าไหร่` **อยู่ในช่องพิมพ์แล้ว** แต่ **ยังไม่มีคำตอบ** — ต้องกด **ส่ง** เองก่อนถึงจะยิงจริง | เปิดแล้วยิงคำถามอัตโนมัติ (เสียเงินโดยไม่ได้กดส่ง) = ไม่ผ่าน |
| E2 | เปิดหน้า **สต็อก** → กดปุ่ม "ถาม AI เรื่องนี้" | ไปหน้า `/ai` โดยมี `ของในสต็อกค้างนานสุดกี่วัน` อยู่ในช่องพิมพ์ · ยังไม่มีคำตอบจนกดส่ง | เหมือน E1 |
| E3 | ปิดผู้ช่วย (consent = `off`/`never_chosen`) แล้วเปิดหน้างบ/สต็อก | **ไม่เห็นปุ่ม** "ถาม AI เรื่องนี้" เลย (เหมือนปุ่มลอยใน AppLayout) | ปุ่มยังโผล่ทั้งที่ยังไม่เปิดใช้ = ไม่ผ่าน |

> 🔴 **E1/E2 คือหัวใจ**: prefill แล้ว **ห้ามส่งอัตโนมัติ** — refresh/back-forward ที่หน้า `/ai?q=…`
> ต้องไม่ยิง request ใหม่เอง เพราะทุก request คือเงินจริงและกินโควตา

---

## วิธีบันทึกผล

ก๊อปตารางนี้ลงบันทึกทุกครั้งที่ยิงชุด (1 บล็อกต่อ 1 รอบทดสอบ) เพื่อเทียบข้ามรอบได้จริง:

```
วันที่:            ____________________
โมเดล (ANTHROPIC_MODEL): ____________________
commit (ท้ายหน้าตั้งค่า): ____________________   ← อ่าน version stamp ก่อนเสมอ (กัน service worker cache ค้าง)
seed verify:       PROVEN __/__  (ต้องครบก่อนเริ่ม)

| ข้อ | รอบ1 | รอบ2 | รอบ3 | สรุป (ผ่าน=ทุกรอบผ่าน) | หมายเหตุ |
|-----|------|------|------|------------------------|----------|
| A1  |      |      |      |                        |          |
| A2  |      |      |      |                        |          |
| A3  |      |      |      |                        |          |
| B1  |      |      |      |                        |          |
| B2  |      |      |      |                        |          |
| B3  |      |      |      |                        |          |
| B4  |      |      |      |                        |          |
| C1  |      |      |      |                        |          |
| C2  |      |      |      |                        |          |
| C3  |      |      |      |                        |          |
| C4  |      |      |      |                        |          |
| D1  |      |      |      |                        |          |
| D2  |      |      |      |                        |          |
| D3  |      |      |      |                        |          |
| D4  |      |      |      |                        |          |
| D5  |      |      |      |                        |          |
```

- **กลุ่ม C ยิง 3 รอบ** (คอลัมน์ รอบ1–3) · กลุ่มอื่นยิง 1 รอบพอ (แต่ถ้าสงสัยให้ยิงซ้ำ)
- "สรุป = ผ่าน" ก็ต่อเมื่อ**ผ่านทุกรอบที่ยิง** · หลุดรอบเดียว = ไม่ผ่าน
- เก็บบันทึกของทุกโมเดล/commit ไว้เทียบกัน — นี่คือทั้งหมดที่ทำให้ "เปลี่ยนแล้วดีขึ้นจริงไหม"
  ตอบได้ด้วยหลักฐาน ไม่ใช่ความรู้สึก
